all:
  vars:
    ansible_user: root

external_endpoints:
  hosts:

main_grp:
  vars:
    ansible_user: root
    site_domain: "wgb.hosts.name"
    local_net4: 10.42.0.0/16
    local_net6: fd:cafe:42::/96
    wg_easy_ipv4_subnet: "{{ local_net4 }}"
    wg_easy_ipv6_subnet: "{{ local_net6 }}"
    wg_easy_ipv4: "10.42.0.2"
    wg_easy_ipv6: "fd:cafe:42::2"
    wg_easy_use_awg: true
  hosts:
    secondary:
      ansible_host: 153.80.184.189

blah:
  hosts:
    main:
     ansible_host: 31.130.144.133

endpoint:
  vars:
    ansible_user: root
  hosts:
